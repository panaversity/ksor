{
  "name": "@ksor-internal/tsconfig",
  "version": "0.0.0",
  "private": true,
  "description": "Shared strict TypeScript base config for the ksor monorepo. Extend it; never fork it.",
  "files": [
    "tsconfig.base.json"
  ]
}
