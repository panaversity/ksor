# `pnpm add` records exact versions (no ^ prefix). Existing ranges are
# deliberate: shared pins live in the pnpm-workspace.yaml catalog, and tool
# devDependencies float within a major on purpose.
save-prefix=""
