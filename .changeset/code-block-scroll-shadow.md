---
"@panaversity/ksor": patch
---

A code block wider than the measure now says so. The edge it can scroll toward
carries a soft shadow, and the platform's scrollbar is replaced by a hairline —
so a command with an aligned trailing comment reads as "there is more" rather
than as text cut mid-word. A block that fits shows nothing at all.
