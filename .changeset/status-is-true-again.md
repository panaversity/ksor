---
---

Repo docs only — outside `packages/ksor`, so no version bump.

`docs/status.md` is the document AGENTS.md names "the only authority on what is
implemented", and both the npm README and the bundled docs link agents to it. It
still described 0.0.7 while 0.0.13 was published: six releases of behaviour
missing, `ksor takedown` listed as unreleased, and an owner action recorded as
outstanding that was completed. It now states what is released, what was
measured, and — plainly — that the vector index is not being used.
